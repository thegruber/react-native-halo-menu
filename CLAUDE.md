@AGENTS.md

## Claude Code

- Use `AGENTS.md` as the shared source of truth for this repository.
- Keep this file short; add durable project rules to `AGENTS.md` unless they are Claude-specific.
- Put private local preferences in `CLAUDE.local.md`, which is ignored by git.
